# exp71 从零 Q learning 判决实验（纯血线估计器对照，用户 2026-09-06 批准"同步进行"）

- **Date**: 2026-09-06  **Status**: done（r2 早停于 49% 预算，判负）
- **Git**: 见 Progress；RunPod Secure L40S；纯血：随机初始化、镜像自对弈、零人类数据、零外部模型
- **Env**: `train_dnn_dqn.py`（exp59 训练器）新增：`--init` 可省略（随机初始化）、`--league` 可省略（四席学习者镜像）、`--temp_schedule`

## Purpose & Hypothesis
用户问题：Q learning 是否让从零训练更可行？分解后（2026-09-06 讨论）：Q 方法命中"归因信噪比"（一步/n 步自举替代 σ≈5,000 的蒙特卡洛回报）
和"样本效率"（回放池复用），不命中"单局点差均衡不防守"。exp59 的反例：从 bc49 热启动的**去锚**纯 TD 掉到对 bc49 0.21（≈纯血水平），
说明 5 万–20 万局尺度的 TD 不含排序信息——但那是热启动+冻结联赛环境，从零+镜像自对弈从未测过。
**假设 H1（估计器有用）**：同预算（100 万局）从零 Double DQN 产物 T=0 对 exp68r3-P3 ≥ 0.50。
**反假设 H0**：≤ 0.45（ε/Boltzmann 探索拆手牌 + Q 尺度被运气方差淹没 → 弱于策略梯度）。中间带 0.45–0.50 = 不分胜负，记录后不再追加预算。

## Method
- 训练器 exp59 `train_dnn_dqn.py` 在线模式（`collect_parallel`，无 store）；无 `--init`（随机）；无 `--league`（`league_plan` 空池 → 四席全学习者，
  `_package_game` 保留四席 episode，数据 4×）；`--self_frac 1.0`（完全在线，Q 的乐观错误自我证伪）。
- 目标：前 5 万局纯 MC（尺度校准），之后 n=10 步 Double DQN，γ=0.995，硬同步每 500 更新，reward_scale 0.05，Huber。
- 探索：Boltzmann(Q) 温度按局数分段线性 `0:1.0,50000:0.1,300000:0.03,1000000:0.01`（随机初始化 logit≈0 → T=1 即均匀随机；
  MC 校准后 Q 差 ~0.01–0.1 尺度单位，需 T≈0.02 才"按 Q 行动"）+ 单点偏离 `single_dev_p 0.04`（保持手牌连贯）。
- 回放：cap 2M 步（fp16 planes ≈ 8 GB RAM），replay_ratio 2、batch 512、lr 1e-4、bf16 更新。
- 预算 = P3 同预算 **100 万局**；架构 cnn_m_r（同 P3）；`games_per_iter 2048`，6 worker，gpu_infer。
- 无 DQfD 锚、无行为策略（纯度）；无 dueling（判决实验只测"换估计器"，不叠加架构变量）。
- 终评（工作站，T=0）：单局配对牌山 Q vs P3（67M n=4000 + 68M n=16000 双段合并）；Q vs exp27A；防守探针 + 暴露席放铳率；风格向量。

## Success Criteria（预注册）
1. **主判据**：Q vs P3 合并 share ≥ 0.50（n=20,000，SE≈0.0035）→ 估计器换法值得继续（下一步 dueling/n 步 critic 混合）；≤0.45 → 关闭"纯 DQN 从零"。
2. 中途健康：MC→TD 切换后 q_mean/target_mean 不发散（|q|<3 尺度单位）、和牌率单调上升到 ≥15%（PPO 从零同期 ≈20%）。
3. 防守探针只记录不判决。
- 预算：≈4.5–5h Secure L40S ≈ **$5，上限 $6**（与 exp70 并行，独立 pod，不影响 exp70 吞吐）。

## Progress
- [09-06 21:55 PDT] 用户批准同步开跑。训练器改动 + `tests/test_dqn_from_scratch.py`；本机冒烟后发射。
- [09-06 21:20 PDT] 本机冒烟 192 局：随机初始化 199 万参数、镜像四席、replay 21×34 fp16（≈89 步/局，2M cap ≈ 3.5 GB）、MC→TD 切换、温度表生效。
  提交 e6f35f2。发射：Secure L40S US-TX-4 pod `tvrd13n1982g0b`（$1.09/h，ssh 195.26.232.177:46107），GPU 数值校验通过，Q 臂 04:20 UTC 起跑。
  心跳/拉取（含 TB 镜像）挂好；TB 加 exp71_Q_LIVE。预计 ≈4.5–5h。
- [09-06 22:45 PDT] **r1 判定配方错误，中止（80 min ≈ $1.5）**。健康检查（本机 T=0，n=600 对）：Q@307k vs 随机初始化 0.711（学到了东西），
  vs P3@100k 局 **0.147**、vs P3@300k **0.060**（同预算差距巨大）。诊断脚本 `q71_gaps.py`（16 局 1,395 个多选状态）：
  原始回报 σ=2.08（千点单位）× reward_scale 0.05 → Q 尺度 0.1；同状态 Q 顶二差中位数 **4.4e-4**、p90 2.9e-3；
  训练温度 T=0.03 下行为熵 1.989 ≈ 均匀熵 1.994（T=0.003 仍 1.90）⇒ **30 万局的行为数据全是均匀随机打牌**，Q 学的是"乱打的价值"。
  同时 bf16 更新前向在 Q≈0.03 尺度上的分辨率 ~1e-4，与顶二差同阶。
  **r2 修正**：①自适应温度 `--target_entropy_frac`（每迭代在 4,096 个回放状态上二分求 T，使行为熵 = 目标比例 × 均匀熵；
  表 0:0.9,50000:0.5,300000:0.25,1000000:0.10），不再依赖 Q 绝对尺度；②reward_scale 0.05→0.5（Q 尺度 ~1）；③去掉 bf16。
  其余不变。教训入 SKILLS：Boltzmann(Q) 探索的温度必须相对 Q 差尺度设定或按目标熵自适应，发射前用 q_gaps 类脚本核对行为熵。
  **在轨判据**（回答"是否在正轨"）：每个里程碑 ckpt 对**同局数**的 P3 ckpt 做 T=0 配对（n=600），share 曲线就是相对策略梯度的学习曲线；
  r1 的 0.06–0.15 = 严重脱轨。
- [09-06 22:40 PDT] **r2 发射**（同 pod `tvrd13n1982g0b`，r1 进程清净、目录归档为 exp71_Q_r1）：提交 45e0e5e；`--target_entropy_frac 0:0.9,50000:0.5,300000:0.25,1000000:0.10
  --reward_scale 0.5`、无 bf16，其余同 r1。迭代 1 95.8 局/s，td 0.24（Q 尺度 ~1 已生效）。心跳/拉取/TB 镜像重挂；新增 **在轨循环** `exp71_track_loop.sh`：
  每个新 ckpt 自动对随机初始化与同局数 P3 ckpt 做 T=0 配对 n=600，结果追加 `experiments/probes/exp71_track.jsonl`。预计 ≈3.5h、≈$4（累计 ≈$5.5，≤$6 上限）。
- [09-07 00:12 PDT] **在轨曲线（r2，T=0，n=600/点）判定平台 → 早停**：
  | 局数 | vs 随机初始化 | vs 同局数 P3 |
  |---|---|---|
  | 51k | 0.694 | 0.124 (P3@100k) |
  | 102k | 0.900 | **0.348** (P3@100k) |
  | 154k / 205k | 0.910 / 0.883 | 0.241 / 0.262 (P3@200k) |
  | 256k / 307k | 0.905 / 0.877 | 0.208 / 0.229 (P3@300k) |
  | 358k / 410k | 0.888 / 0.881 | 0.192 / 0.175 (P3@400k) |
  | 461k | 0.860 | 0.184 (P3@500k) |
  r2 修正生效（r1 同局数 vs P3 仅 0.06–0.15；r2 在 10 万局达 0.35），但 **10 万局后对随机初始化平台在 0.86–0.91，对 P3 随 P3 继续变强而单调下滑**。
  Q 终点对 P3 终点的上界 ≈0.18 ≪ 0.45 关闭线，剩余 54 万局不可能翻转 ⇒ 按预注册反假设 H0 **早停**（省 ≈$1.6），
  终评用 games_460800.pt 对 P3 终点/exp27A 双段 20k 对 + 防守探针，写 Results。td 0.23 / q +0.02 / y +0.03 尺度正常、T 自适应到 0.006（熵目标 0.25→ 已近贪心），
  即"日志健康、学习停滞"——与 exp59 结论一致：TD 在此尺度不含足够排序信息，且 PPO 每样本一次的策略梯度反而比 replay ratio 2 的 Q 回归学得快。

## Results
| 指标 | exp71 r2 Q@461k 局（T=0） | 基线 | 判据 |
|---|---|---|---|
| vs P3 终点（1M 局）双段 20k 对 | **0.1705 ± 0.0027**（mean_diff −12.2k） | — | ≥0.50 成立 / ≤0.45 关闭 → **关闭** |
| vs exp27A 双段 20k 对 | 0.1763 ± 0.0027 | P3 vs exp27A 0.4922 | — |
| vs 随机初始化（n=600） | 0.86–0.91，10 万局起平台 | — | 学到了东西但停滞 |
| 在轨曲线 vs 同局数 P3 | 0.35 (100k) → 0.18 (461k) 单调下滑 | — | 早停依据 |
| 防守探针（800 局） | defense_iq −0.049，暴露席放铳 0.209 | P3 +0.024 / 0.195 | 只记录 |
| r1（固定温度）同局数 vs P3 | 0.06–0.15 | — | 配方错误，行为=均匀随机 |
| 费用 | r1 $1.5 + r2 ≈$2.2 = **≈$3.7** | 预算 $5，上限 $6 | |

## Conclusion
**反假设 H0 成立：从零 Double DQN（exp59 训练器，镜像自对弈，回放 2M/ratio 2，自适应 Boltzmann 探索）同预算下远弱于策略梯度**
（0.17 vs P3，Elo 差约 270）。学习在 10 万局后停滞，而 PPO 每样本只用一次却持续改进到 100 万局。与 exp59"去锚纯 TD 不含排序信息"一致：
在 σ≈2（千点单位）的终局回报下，Q 回归的动作排序信号被淹没，回放复用并不能补回来。r1 教训（温度必须相对 Q 差尺度/按目标熵自适应）已入 SKILLS。
**纯血线上"换估计器为 Q learning"这一杠杆关闭**；价值方法若再上场，只能以 PPO critic 的回放/n 步自举形态出现（阶段 B 第 2 项降为低优先级）。

## Next Steps
- exp72：决策级反事实 rollout 优势（$200 计划阶段 B 第 1 项），目标先在单局配方上（exp70 90k 探针防守未涌现；等其半庄终评决定目标）。
- 不再为纯 DQN 从零追加预算。

## Artifacts
| Path | Size | Description |
|---|---|---|
| `experiments/exp71_Q/games_460800.pt`（+GCS `exp71_Q/`） | 8 MB | r2 终点（早停）ckpt |
| `experiments/exp71_Q/` ckpt 每 51,200 局 ×9、train_log.json、tensorboard | — | r2 全程 |
| `experiments/exp71_Q_r1/`（+GCS） | — | r1（固定温度）ckpt ×6 + 日志，作反面样本 |
| `experiments/probes/exp71_track.jsonl` | — | 在轨曲线（vs 随机 / vs 同局数 P3） |
| `experiments/probes/exp71_final.json`、`exp71_defense.json` | — | 终评 + 防守探针 |
| scratchpad `q71_gaps.py`、`eval71_track.py`、`exp71_track_loop.sh` | — | 诊断/在轨工具（未入库） |
