# dnn_exp36_hrf_lowent_20260824 — HRF × 恒定低熵、长视野、bf16+amp 提速（从零）

- **Date**: 2026-08-24  **Status**: launching（先验证吞吐，过关后 --resume 续到正式长度）
- 动机：b2_4/b2_4ext（handset 恒定 0.01）展现自组织分阶段探索 + 二次转型，T1 已破 1044；
  假设该性质随表达力增强（容量/混叠假说：expressivity 抑制棘轮全局化，[[handset-constant-entropy-recipe]]）；
  HRF（23.2M，8 层交叉注意力，比 handset 更强的局面个体化能力）是下一格验证。
  用户要求：发射前先优化 rollout+训练效率——本地已验证 bf16_infer（rollout 本地 1.12×，云端待测）
  + amp_update（更新步矩阵乘法本地 B=512 下 1.51×，云端待测，本地因显存不足无法测真实 batch=4096）。

## Method
`--arch hrf_xl_v4 --entropy_coef 0.01`（恒定，无 schedule）+ `--bf16_infer --amp_update`；
其余同冠军配方；对照 = exp30（同架构、冠军 schedule，同纪元，24.0 games/s 已知基线）。

**两段式发射**：
1. 验证段：短 total_games（如 20000），读 `rollout_s`/`update_s`/`games_per_sec` 打点，
   确认吞吐相对 exp30 fp32 基线（24.0/s）的实际提升、无 NaN/数值问题。
2. 正式段：验证通过后 `--resume` 同一 checkpoint，`--total_games` 改为 2,000,000（长视野，呼应
   b2_4ext"自组织平台需要预算才能变现"的教训），games 计数器不清零——同一条从零血统连续训练。

## Success Criteria（正式段）
1. 吞吐：games/s 相对 exp30 fp32 基线有实测提升（数字待验证段给出，不预设倍数）。
2. 复现自组织现象：熵是否出现 ≥0.4 的自持平台、是否出现类似 b2_4ext 的二次转型（熵回升 + 副露/立直再平衡）。
3. Elo：T1 vs exp30（冠军 schedule 对照）；长视野后再挑战 exp31-5（cnn_m 冠军配方 1031.9）。
4. 数值健康：amp_update 全程无 NaN/inf，approx_kl 与 fp32 时代量级一致。

## Progress
- 2026-08-24 验证段完成（20000 局，10 迭代）：**39.1 games/s，比 exp30 fp32 基线（24.0）快 1.63×**。
  拆分：rollout_s 稳定 ~12.1s/iter，update_s 稳定 ~27.0s/iter（收益主要来自 amp_update，更新步本就是大头）；
  approx_kl 全程 0.0015–0.0049（远低于 0.03 目标线），无 NaN，无漂移。另有 ~13.3s/iter"其他"开销
  （GAE/日志/ckpt 保存）未被打点覆盖，记为后续优化线索，不阻塞本次发射。
  验证机 mahjong-g4-e36 命中 total_games 后自动关机（预期行为）；ckpt 转存
  gs://llm-mahjong-experiments/resume/dnn_exp36_hrf_lowent_validation_20k.pt，删除旧机。
- 2026-08-24 正式段发射（mahjong-g4-e36full，同 SHA ec117a0，--resume，games=20480 起，
  total_games 2,000,000，milestones 100k/500k/1M/1.5M/2M）。

- 2026-08-24 事故：正式段沿用了验证段的同一个 run 名 → 验证段留下的旧 `games_final.pt`（2万局）
  仍在同一 GCS 路径下，`watch_run.sh` 只判存在不判局数，resume 后几分钟内误判 DONE，guard 的删除循环
  与 close_run.sh 是否成功无关，险些删掉正在训练的 VM（VM 实际已删，但 resume checkpoint 完好，
  只损失重启后几分钟未存档的计算，无数据丢失）。**修复**：正式段改名 `dnn_exp36_hrf_lowent_full_20260824`
  重发；`run_guard.sh` 通用脚本改为删除前必须确认 close_run.sh 打印 CLOSE_DONE，不再无条件删除。
- 2026-08-24 中段 ladder（60 副/点，±26，噪声大）：@1.59M **874.2**、@1.64M **899.4**、@1.68M **855.4**。
  **修正此前过早的乐观表述**：我先前基于风格轨迹写下"HRF 上恒定低熵优于冠军 schedule"，
  但 Elo 数据不支持——exp30（schedule，终局 500 副精测）852.1±12.4，exp36 中段徘徊 855–900，
  **两者在同一水平,没有清晰差异**。风格上 exp36 确实逃出副露锁死（call 92.7%→40%、
  riichi 3.9%→17.4%），但强度没有同步兑现。**这是 b2_4ext 教训的第二次重演：
  风格转型 ≠ Elo 提升**（第一次是 handset 二次转型后终局回落到打平）。
  趋势外推：agari 斜率 +0.0022/100k（近 500k，略有加速），要追上冠军 0.198 需再 ~4.3M 局
  = 剩余预算 4 倍，非可行路径。低和牌率更像"收敛到弱均衡"而非"训练不足"。
  待 vs_anchors（生态无关读数）+ defense_iq 判定：低 agari 是镜像生态假象还是真实弱。
- 2026-08-24 **生态假设被证伪**（vs 三锚点 600 局 T=0，生态无关读数）：
  | | 镜像自对弈 | **vs 锚点** |
  |---|---|---|
  | agari | 11.4% | **9.8%（更低，未回升）** |
  | houjuu | 7.2% | **14.8%（翻倍）** |
  | 流局 | 52–59% | 18.8% |
  面对不会跟着龟的固定对手，**和牌率没有回升反而更低，放铳率翻倍到 14.8%（高于人类带 11–13%）**。
  → 镜像下的"低放铳"是全桌互相不喂牌的生态假象，不是防守能力；低和牌率是真实弱，不是假象。
  defense_iq **0.011 ≈ 0**，且弃胡率跨危险等级basically 平坦甚至倒挂
  （weak 0.298 / mid 0.283 / tenpai 0.287——对手越危险反而弃得越少）：**零防守判别力**。
  **结论：exp36 在 1.68M 处是真实弱（Elo 855–900 与之一致），不是被生态压低的强模型，
  也不是训练不足**——三条证据（Elo 平台、生态无关读数、defense_iq）汇聚。
- 2026-08-24 **收官（VM 撞 12h flex 上限自动删除，停在 1,812,480/2,000,000 = 90.6%；
  latest.pt 完好，转存 games_final.pt 后本地精测）**：
  **T1 = 896.2 ± 12.1**（500 副）。对照：exp30（HRF×冠军 schedule）852.1、
  cnn_m 冠军配方 exp31-5 1031.9、exp31-6（cnn_m 恒定低熵）1000.7。
  竞技场 vs exp22r2：−5876.8±1316.6（190:556，明显落后）。greedy_agree 0.580。
  ## 判据结算
  1. 吞吐提升：**✅ 达成**（39–43 games/s vs exp30 fp32 基线 24.0，1.63–1.8×；
     bf16_infer + amp_update 端到端有效，数值健康 KL 全程 <0.025）。
  2. 复现自组织现象：**✅ 现象成立**（熵平台 ~0.70 → 逃出副露锁死：call 98.9%→40%、
     riichi 0.1%→17–22%、win_turn 15.9→13.5；末段熵回升 0.55→0.70 = 二次转型信号）。
  3. Elo：**❌ 失败**。896.2 高于 exp30 的 852.1（+44，约 2.6σ，恒定低熵在 HRF 上确实优于
     schedule），但**远低于 cnn_m 两条线（1031.9 / 1000.7）**——HRF 架构本身在 1.8M 预算内
     不具竞争力。
  4. 数值健康：**✅**（bf16 全程无 NaN/inf）。
  ## 结论
  **架构×熵机制交互确认成立但无实用价值**：HRF 上恒定低熵 > 冠军 schedule（896 vs 852），
  方向与 cnn_m 相反（那边 schedule 1031.9 > 恒定 1000.7）——这是干净的交互效应，值得作为
  论文的机制证据。但**两条 HRF 线都远弱于 2M 参数的 cnn_m**，23.2M 参数换来 −100~−140 Elo。
  容量/混叠假说的"表达力买来自组织逃逸"部分成立（HRF 确实逃出锁死），
  但"逃逸 ⇒ 更强"不成立（第三次重演 b2_4ext 教训：风格转型 ≠ Elo 提升）。
  **HRF 路线暂停**，不进入冠军谱系；后续注意力架构探索需先解决"为什么大模型在此任务上吃不到
  参数红利"这个更根本的问题（候选诊断：样本效率/优化器超参/river token 表示的信息密度）。
