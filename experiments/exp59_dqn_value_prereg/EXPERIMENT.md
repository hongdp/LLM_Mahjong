# exp59 — 价值方法线 v1：DQN on 人类先验（单局刻度）

- **Date**: 2026-08-31  **Status**: done（2026-09-02 收官；后继 exp60）
- **Git**: 基线 fcdbe3a；设计 [designs/design_dqn_value.md](../designs/design_dqn_value.md)
- **Env**: 本地 4080（冒烟）；正式 tranche 走 RunPod 3090Ti（exp58 runbook）

## Purpose & Hypothesis
exp55-D 证明策略梯度读不动排位价值信号（EV 0.09、漂移弥散、强度打平）。本系列检验：
**价值方法（DQN：直接回归 Q(s,a)，策略=argmax）能否在同一引擎与先验上有效学习**。
v1 只回答机器问题（单局刻度、常规奖励）；W 信用接 Q 目标的排位版是 v2。
结构性论点：①目标策略=贪心=部署形态（无采样税错位）；②回放重复采样磨高方差回报；
③league 冻结 T=0 三席 ⇒ 环境平稳（Q 收敛前提）。

## Method
- 新训练器 `scripts/train_dnn_dqn.py`：rollout 侧零改动（collect_parallel + GPU 批推理 +
  Boltzmann(Q,T=1) 行为），episodes → (s,a,r,s',mask',done) transitions → 环形回放（1M）
  → Double DQN + Huber，目标网硬更新。
- 热启动：bc49 trunk + policy 头照抄为 Q 头（argmax 保序 ⇒ 行为从 bc49 贪心起步）。
- 奖励：registry 原样，scale 0.05；γ=0.995。
- 对手：league={bc49, exp46I} 冻结 T=0，学习席 ×1（league_frac=1, learner_seats=1）。
- 监控：TD loss、Q 均值/回报均值比率、贪心快评（对 bc49）、风格九项。

## Config（冒烟）
batch 512、lr 1e-4、target_every 2000 次更新、回放 1M、每迭代 rollout 512 局 +
若干梯度步（更新数 : 新样本数 ≈ 1 : 8 起步）。快照见本目录 config 文件。

## Config（正式 tranche-1，2026-08-31 晚，RunPod 3090Ti community）
v1.2 配方 + **margin 退火**：`--behavior_ckpt bc49 --margin_coef 1.0
--margin_schedule 0:1.0,80000:0.3,140000:0.1`（前 80k 局锚死排序、TD 校准量纲；
之后两级放松，看 TD 敢不敢推翻先验的局部决策）。200k 局、workers=保证核数、K=32、
replay_ratio 2、n-step 10、mc_until 20k、reward_scale 0.05。
预算 **≤$1**（估 ~45 分钟 ≈ $0.25）。runbook：cgroup 核数校验、expandable_segments、
pip 补 tensorboard、完赛后由工作站侧调 API terminate（kill 1 不停表）、心跳挂托管后台任务。

## Success Criteria（预注册）
- **冒烟（本地，~50k 局）**：①TD loss 收敛趋势（不振荡发散）；②Q/回报均值比率 ∈ [0.5, 2]
  （>2 = 过估计警报）；③贪心行为不崩：vs bc49 快评（n=400 单局，T=0）≥ 0.45。
- **正式（云，200k 局）**：贪心 vs bc49 单局 n=2000 ≥ 0.48 ⇒ 「DQN 在本引擎上能学」成立，
  批准 v2（半庄 + W 信用目标）；< 0.45 ⇒ 诊断（过估计/覆盖/量纲）后再决定。
- 失败也有价值：与 PPO 的对照钉死「是信号问题还是优化器问题」。

## Progress
- [08-31] 设计定稿；训练器实现中。
- [08-31 20:35] 发射两坑：cfg 漏 `action_space` → m46 网配 374 宽掩码 → 空 multinomial
  device assert（修复：`space_of_arch`）。
- [08-31 20:45] **v1 冒烟（自驱动 Boltzmann）判负，机制清晰**：50k 局 10.4 分钟（80 局/s），
  TD 0.012 收敛、q̄≡ȳ 无过估计——但 league 分差代理一路恶化（−1554→−3084），
  贪心终评 **vs bc49 = 0.142±0.011（−9697 分/对）**，判据③（≥0.45）惨败。
  **病理**：MC 重标定把 Q 压到 ±0.1 → Boltzmann T=1 近似均匀 → 行为滑向随机 →
  Q 在垃圾数据上自洽（低 TD 是假象），热启动的动作排序被噪声梯度冲掉。
  设计文档预警的「量纲错位崩坏」精确兑现，按预注册退路转 v1.1。
- [08-31 20:50] **评测度量坑（差点误判为大胜）**：单局模式 `play_pair_vector` 的
  `games[].a_pts` 是 **A 两席点数总和**（≈50k 恒正），只有半庄模式才是 uma 差——
  初版评脚本读出 share=1.0。**A/A 体检（bc49 自打 = 0.500/0.0）当场揭穿**；
  正确口径用返回的 `sc`（复式对胜份）。「新指标先跑 A/A」规则再立一功。
- [08-31 21:00] **v1.1 发射**：`--behavior_ckpt bc49` 冻结行为 = bc49 T=1 采样，Q 网纯离策略
  学习（= 对 bc49 做策略评估 + 单步贪心改进；策略改进定理给出明确预期：Q̂ 准 ⇒ 贪心 ≥ bc49）。

- [08-31 21:40] **v1.2（+DQfD 大间隔损失，margin_coef 1.0/m 0.05）冒烟三判据全过**：
  TD 0.03 稳定 ✓；q̄≡ȳ 校准 ✓；**贪心 vs bc49 = 0.4985±0.0158（+124 分/对，n=1000 对）** ✓。
  修复曲线 0.142（v1 自驱动）→ 0.213（v1.1 冻结行为）→ 0.4985（v1.2 +排序锚）。
  机制结论：**纯 TD 在 5 万局尺度重建不了监督先验的动作排序；margin 锚保排序、TD 只调量纲
  与例外**——正式 run 需测 margin 退火（锚太紧 Q 永远超不过先验）。

- [09-01 20:45] **正式 tranche-1 发射**：pod `lvo4410fz9tian`（3090Ti community，EPYC 7663，
  cgroup 23 核），SHA 3fa5e19，v1.2 配方 + margin 退火 1.0→0.3@80k→0.1@140k，200k 局。
  首迭代 60 局/s、TD 1.04→0.27、行为代理 −70 稳定。心跳=托管后台任务（会叫醒）、
  TB `exp59_t1_LIVE`、每 30 分钟 ckpt 回拉。坑：家用上行 ~1Mbps，两个锚 ckpt（32MB）
  scp 走了 12 分钟——以后锚点从 GCS 拉到 pod，不经工作站。

- [09-01 22:00] **tranche-1 完赛**：200,192 局 / 66.5 分钟（50 局/s，23 核宿主），11,561 次更新，
  margin 1.0→0.3@82k→0.1@144k 按计划执行，TD 收敛 0.019。pod 已 terminate（实花 ~$0.30），
  ckpt/日志/TB 回收 sha256 一致（`experiments/exp59_t1_ckpts/`，主检出）。
  **TB 暴露训练器 bug**：MC→TD 切换（20k）后第一次目标网硬同步落在 ~35k，期间自举用的是
  bc49 原始 logits 尺度的目标网 → target_mean≈+1.0、q_mean≈0.78 的 16k 局假目标高台，
  第一次同步后坍回。已修（切换瞬间同步 + target_every 2000→500，commit 4214f5c）。
  **本 run 的 Q 曲线不算干净样本**，终评见下。
- [09-01 22:05] v1.3 LP-FT 冒烟发射（冻 trunk 30k 局 → 解冻 trunk lr×0.1，无 margin，60k 局）；
  ConvFormer46 的头是结构化四层（type_head/slot_*，3,542 参数），按名字集合识别。

## Results

| 冒烟判据（预注册） | 目标 | v1 | v1.1 | v1.2 | 判定 |
|---|---|---|---|---|---|
| ①TD 收敛 | 不发散 | ✓(假象) | ✓ | ✓ | ✓ |
| ②Q/回报校准 | 比率∈[0.5,2] | ✓ | ✓ | ✓ | ✓ |
| ③贪心不崩 | vs bc49 ≥0.45 | 0.142 ✗ | 0.213 ✗ | **0.4985 ✓** | **✓** |

三次冒烟各 50k 局 / ~11 分钟 / 本地 4080（80 局/s，rollout 栈零改动复用成立）。

| 正式判据（预注册） | 目标 | tranche-1（200k，margin 退火，含目标网 bug 段） | 判定 |
|---|---|---|---|
| 贪心 vs bc49（单局，n=1000 复式对） | ≥0.48 | **0.4985 ± 0.0158，−430 分/对**；A/A 对照 0.500/0.0 | ✅ 不劣于；**零增益** |

| **v1.3 LP-FT 冒烟**（冻 trunk 30k→解冻 ×0.1，**无 margin**，60k 局） | 对照 | **0.208 ± 0.013，−8422 分/对** | ❌ 与 v1.1（0.213）同款崩 |

| **v1.4 单点偏离**（bc49 贪心行为 + 每局≤1 手 T=1 偏离 p=0.04，margin 1.0→0.3@25k，60k） | 覆盖 | **0.493 ± 0.016，−776 分/对** | 平（与 0.4985 同噪声带） |

| **v1.5 在线 + 四席 + 偏离**（self_frac 1.0、all_seats、single_dev 0.04、margin 1.0→0.3@25k、replay_ratio 0.5，60k） | 覆盖+样本+自证伪 | n=1000：0.5115±0.016；**n=4000 复评（新种子段）：0.4901 ± 0.0079，−142 分/对** | 平（1000 对的 +0.7σ 是采样噪声；同一 ckpt 两批差 1.2σ，正常） |

| **v1.6-A 零锚续训**（init=正式 run 校准 ckpt，mc_until 0，margin 0，在线+四席+偏离，60k） | 去锚是否漂移 | **0.4474 ± 0.0079，−1757 分/对**（n=4000） | ❌ 显著退化（−6.7σ）：校准后去锚仍缓慢漂移——**噪声漂移持续存在，小锚必须常驻** |

**v1.3 的决定性含义**：只训 3,542 个头参数、trunk 一动不动，贪心照样崩到 0.21——
排序的流失发生在**头本身**（线性打分器按噪声回报目标重拟合），与 trunk 表征是否被扰动无关；
且 MC 阶段（0–20k，trunk 仍冻结）就已完成这场流失。**头-only ≈ 全微调 ≈ 0.21**
⇒ 可训参数量不是变量，"逐层解冻"议题关闭；**目标里没有排序信息**（信噪比）才是根，
DQfD 排序锚是目前唯一能保住先验的东西。MC→TD 切换同步修复在本 run 验证生效（无 +1.0 高台）。

读法：与 50k 冒烟（0.4985）同值——多 150k 局、margin 松到 0.1，贪心策略仍与 bc49 不可分。
Q 头只是重新推导了 bc49 的排序，TD 没有在任何可测处推翻先验。
（`probes/exp59_t1_vs_bc49.json`）

## Conclusion（2026-09-02 收官，Status: done → 后继 exp60）

**机器判据过（贪心 ≥0.48），增益判据不过：v1 系列七个臂、正式 200k 局，贪心 vs bc49 全部落在
0.49–0.50（有锚）或 0.21/0.45（无锚）；价值方法在本引擎上"能学、不毁值、不超越"。** 六条钉死的机制：
1. **热启动量纲错配 → 压缩期覆写排序**：log-概率头（±5）回归到回报尺度（±0.1）时，σ≈0.8 的样本噪声把
   排序抹平（v1.1/v1.3 → 0.21）；冻结 trunk 也一样（头-only ≈ 全微调）⇒ 排序流失在头本身，
   可训参数量非变量，逐层解冻议题关闭。
2. **纯 TD 目标在 5 万–20 万局尺度不含排序信息**：有锚 = bc49 排序的重参数化（0.4985），无锚 = 噪声排序；
   校准后去锚仍缓慢漂移（v1.6-A −6.7σ）⇒ **锚必须常驻**，可调的是间隔 m 不是有无。
3. **DQfD 锚兼任过估计控制**：无锚时 MC→TD 切换处出现对未训动作的 max 偏差（v1.3 +0.036），有锚全无。
4. **行为—价值耦合的正反馈崩溃**：Boltzmann(T=1) 对重标定后的 Q≈均匀 → 行为随机 → Q 在垃圾数据上"低 TD 收敛"
   （假收敛教材，0.142）。行为策略必须与 Q 的尺度解耦（冻结先验 / 尺度自适应温度 / ε）。
5. **覆盖是改进的必要条件、样本效率是充分条件**：单点偏离（0.493）、在线采集+四席（0.4901）都没动针，
   与 (σ/Δ)² 的样本账一致——真正的杠杆是配对牌山差分（v2/v3 议题）。
6. **训练器缺陷两处并已修**：MC→TD 切换未同步目标网（正式 run 16k 局 +1.0 假目标）；重启时按局数
   计的 MC 期被积压一次性跳过（改按更新数）。
**方法论沉淀**：新指标先跑 A/A（单局模式 `games[].a_pts` 是两席点数和，差点误判 share=1.0 为大胜）；
1000 对的 ±0.016 不足以判 >0.5（v1.5 的 0.5115 复评为 0.4901）。
**下一步**：exp60 演员/学习者解耦 + 历史联赛 + 持久对局库（用户架构），在同一份数据上做 n-step/目标周期/
历史混合/配对差分的消融。

## Artifacts
| Path | Size | Description |
|---|---|---|
