# exp66 搜索→蒸馏试点：隐藏信息重采样 + bc49 rollout 给争议决策打动作价值标签，信噪比够不够

- **Date**: 2026-09-04  **Status**: done（试点判负）
- **Git**: 见 Progress；本机 CPU 多进程（0 元）
- **Env**: `scripts/rollout_label.py`（本实验新增）：PyMahjongTable 深拷贝 + 隐藏牌重采样（determinization）+ bc49 贪心续打

## Purpose & Hypothesis
文献（docs/literature_review_2026-09.md §3/§4）：决策时搜索 + 蒸馏回网络是唯一有 >+100 Elo 证据的路径（piKL 国际象棋 >85%
胜原模仿网；Ruoss 动作价值标签 +250–490 Elo；Suphx pMCPA 66%）。在花钱搜索之前先回答最便宜的问题：
**用我们的引擎做 PIMC 式 rollout，给 bc49 的争议决策（top-2 logit 间隔 <1.0，占决策 26%，exp61）标注 Q̂(s,a)，标签的信噪比是多少？**
假设：M=64 个重采样下，≥30% 的争议决策能把 top-2 动作分开（|ΔQ̂| > 2·SE），且 Q̂ 的偏好与 bc49 自身偏好的一致率随
bc49 间隔单调上升（标签有意义的必要条件）。反假设：每决策价值差 ≪ 单局 σ（8–10k 分），M=64 下几乎无法分辨 → 蒸馏需要
M≥10³/决策，成本不可行。

## Method
1. bc49 贪心自对弈（本机），自摸回合多选决策中 top-2 间隔 <1.0 的状态深拷贝入库（目标 500 个，来自 ~100 局）。
2. 每个状态：取 bc49 概率 top-3 动作；每个动作 M=64 次：重采样隐藏信息（未立直对手的暗手 + 牌山 + 未翻的岭上/宝牌/里宝槽位，
   立直家手牌保持不变，赤五随牌走）→ 强制执行该动作 → 四席 bc49 贪心（CPU batch-1）续打到本局结束 → 记我方本局点差。
   Q̂(s,a) = 均值，SE = 样本标准差/√M。
3. 指标：①可分辨率 = |Q̂(a1)−Q̂(a2)| > 2·SE 的状态占比；②Q̂ 偏好 vs bc49 偏好一致率，按 bc49 间隔分桶（<0.25 / 0.25–0.5 / 0.5–1.0）
   及对照桶（间隔 ≥2 的稳定决策 50 个，一致率应接近 1）；③每标签 CPU 秒数（外推到 10k / 100k 决策的成本）；
   ④点差 σ 的经验分布（校准 exp59 的 (σ/Δ)² 论证）。

## Success Criteria（预注册）
- 可分辨率 ≥30% 且稳定桶一致率 ≥85% → 标签有信息、成本可算：进入 exp66-b（1 万决策蒸馏进 Q 头，头对头 n=4000）。
- 可分辨率 <10% 或稳定桶一致率 <70% → PIMC 标签在本引擎/本 M 下无信息（或 determinization 偏差过大），路线 ④ 降级，
  记录每标签成本与 σ。
- 中间档：报告 M 的边际效应（M=64 vs 前 16 个样本），估计达标所需 M。

## Progress
- [09-04 01:12] git b188623：`scripts/rollout_label.py` + 重采样不变性测试（136 牌多重集、我方手牌/河/宝牌不变）；冒烟 6 状态 ×3×4 = 6 秒。
- [09-04 01:15–01:50] 试点：120 局 → 5,998 个自摸回合多选决策，取 500 个争议（间隔 <1.0）+ 50 个稳定（≥2.0），
  top-3 动作 × M=64，20 进程 CPU，**2021 秒 = 3.68 秒/状态**（每次 rollout ≈ 0.02 秒·核）。

## Results

| 指标 | 争议决策（n=500） | 稳定对照（n=50，间隔 ≥2） | 判据 |
|---|---|---|---|
| 可分辨率 \|ΔQ̂(top1−top2)\| > 2·SE | **6.0%**（间隔分桶 4.9 / 7.4 / 5.7%） | 24.0% | ≥30% ❌（<10% 档） |
| Q̂ 偏好 = bc49 top-1（对 top-2） | 52.0% | **66.0%** | 稳定桶 ≥85% ❌（<70% 档） |
| Q̂ argmax（top-3 内）= bc49 top-1 | 34.8% | 60.0% | — |
| 中位 \|z\| | 0.74 | 1.02 | — |
| 单次 rollout 本局点差 σ | **≈3,700 分** | ≈3,800 分 | — |
| M 的边际（M=16 → 32 → 64） | 可分辨 7.2 → 5.8 → 6.0%（噪声内） | 14 → 20 → 24% | 线性外推：稳定桶到 85% 需 M ≳ 10³ |

## Conclusion
**PIMC 式 rollout 标签在 M=64 下没有信息。** 争议决策里 Q̂ 对 bc49 自身偏好的一致率 52%（掷硬币），连 bc49 很有把握的稳定决策
也只有 66%。原因就是 exp59 的 (σ/Δ)² 论证的直接量化：单次 rollout 的本局点差 σ ≈ 3,700 分，M=64 时 SE ≈ 460 分/动作，
而动作间真实价值差是几十到几百分 → 要把 SE 压到 ~100 分需要 M ≈ 1,400/动作，即每状态 ≈ 80 秒·20 核，1 万决策 ≈ 9 天工作站 CPU，
10 万决策不可行。determinization 的系统偏差（立直家手牌固定、弃听/振听不重算）在这个噪声下根本还看不见。
→ 路线 ④（搜索→蒸馏）**在"全局 rollout 打标签"这个形态下关闭**。文献里可行的形态都不是全局 rollout：piKL 用价值网截断、
Ataraxos 用 1000 次 rollout 但每步只做一次决策而非离线标注全部决策、Ruoss 用 Stockfish 当老师。对我们，前提是一个
方差远小于 3,700 分的价值网（oracle critic 看牌山 + 对手手牌，文献路线 ⑤ 的前置条件），没有它搜索无从谈起。

## Next Steps
- 关闭 exp66-b（蒸馏）；把 σ≈3,700 / (σ/Δ)² 的量化写入 FINDINGS，作为价值方法线（exp59/60）病根的实测锚。
- 若未来做 oracle critic：先用本标注器的 M=64 数据做"critic 预测 vs rollout 均值"的相关性检验（数据已落盘）。

## Artifacts
| Path | Size | Description |
|---|---|---|
| experiments/probes/exp66_labels_M64.json | ~2MB | 550 状态 × 3 动作 × 64 次 rollout 的本局点差（含 bc49 logits、间隔、种子） |
| scripts/rollout_label.py, tests/test_rollout_label.py | — | 标注器（deepcopy + 隐藏牌重采样 + 贪心续打） |
