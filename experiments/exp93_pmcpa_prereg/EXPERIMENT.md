# exp93 运行时策略适应（Suphx pMCPA 的纯血版）：每局开局用 K 条确定化自对弈微调策略副本（2026-09-14，$0）

- **Date**: 2026-09-14 23:30 PDT  **Status**: done（H0：与打乱优势的对照无差）
- **Git**: `pmcpa_probe.py`（scratchpad）；复用 `Table.determinize` + `VecEnv.from_tables`（exp86）；纯血（无人类数据，回滚策略 = 自身）
- **Env**: 本机 4080；对手 3×M（T=0），配对控制 = 未适应的 M 坐同一席同一牌山

## Purpose & Hypothesis
纯血线训练侧与目标侧全部关闭后，剩下唯一有文献大效应的机制是 Suphx 的 run-time policy adaptation（适应版对非适应版 66% 胜率）：开局时按当前手牌采样 K 个隐藏信息样本，自对弈到局末，用这些轨迹对策略副本做几步策略梯度，再用副本打这一局。它和 exp86 的搜索不同：不是在单个决策上比较回滚均值，而是把整局的条件分布信息压进参数。
- **H1**：适应版对未适应版同牌山配对点差 ≥ **+150 分/局**（z≥3，2,000 局），且方向在两组超参下一致 ⇒ 值得做对 bc49 的 20k 对评测（需要 Python 引擎侧的适应包装，另行工程）。
- **H0**：≤ +50 或两组符号不一致 ⇒ 关闭；纯血线推理侧也判决完毕。
- 反解释：适应改变了 23% 局的结果（冒烟），若均值为负说明几步 PG 在 1.8k 样本上主要是噪声注入。

## Method
- 触发：seat 0 第一次切牌决策（`discard_count[0]==0`）；K ∈ {128, 256} 个 `determinize(0, seed)`；首动作按基策略 T=1 采样；四席都用基策略 T=1 自对弈到局末；取 seat 0 轨迹（≈1.8k 样本/局），PPO-clip（0.2）+ 归一化优势（基线 = 均值），Adam lr ∈ {1e-4, 3e-5}，steps ∈ {4, 2}，梯度裁剪 1.0；副本只用于本局 seat 0 的 TURN 决策（副露/荣和判断仍用基策略）。
- 评测：2,000 局，seat 0 适应版 vs 3×M，与 seat 0 基策略同 seed 配对。
- 成本：0.28 s/局适应，$0。

## Success Criteria（预注册）
1. H1 如上。2. 两组超参一致性。

## Progress
- [09-14 23:30 PDT] 冒烟 30 局：0.28 s/适应、1,844 样本、23% 局结果改变；配对差 −483 ± 277（噪声）。2,000 局两组运行中。
- [09-14 23:20 PDT] 组 1（K=128、4 步、lr 1e-4）2,000 局：配对 **+134 ± 71 分/局**（z≈1.9），适应 1,977/2,000 局、每次 1,860 样本、0.26 s；22% 局结果改变；适应版均值 +223 vs 控制 +89。方向正、未达 +150 且未显著。等组 2（K=256、2 步、3e-5）。
- [09-14 23:40 PDT] 组 2（K=256、2 步、lr 3e-5）：配对 **+5 ± 46**，只 10.7% 局改变——更新太小等于没动。两组不同向幅度，判据未达。追加三组区分"信息 vs 扰动"（$0，各 2,000 局）：①组 1 复现（新 seed 920000）；②更大步（8 步）；③**打乱优势的对照**（同扰动量、优势与样本解耦）——若 ① ≈ +130、② ≥ ①、③ ≈ 0 ⇒ 效应是信息；若 ③ 也正 ⇒ 只是扰动/噪声。
- [09-15 00:25 PDT] 三组对照（各 2,000 局）：①组 1 复现（seed 920000）**−55 ± 74**；②8 步更大更新 **−61 ± 82**；③打乱优势对照 **+63 ± 71**。

## Results
| 配置 | 配对差（分/局） | 局结果改变 |
|---|---|---|
| K128 / 4 步 / 1e-4（组 1，seed 910000） | +134 ± 71 | 22% |
| 同上，seed 920000（复现） | −55 ± 74 | 21% |
| K128 / 8 步 / 1e-4 | −61 ± 82 | 29% |
| K256 / 2 步 / 3e-5 | +5 ± 46 | 11% |
| **打乱优势对照**（K128 / 4 步 / 1e-4） | +63 ± 71 | 21% |
- 五组合并（真实优势四组）：≈ +6 ± 35；对照 +63 ± 71 与真实组无差。

## Conclusion
- **H0**。组 1 的 +134 是噪声：换 seed 复现为负，更大更新为负，把优势打乱（只保留同量级参数扰动）得到同样的读数。开局 K=128–256 条确定化自对弈（1.8–3.7k 样本）提供的信息量不足以让几步策略梯度产生方向一致的改进；Suphx 报告的 66% 依赖其 SL 起点策略与更大的采样规模，在我们的强度与样本量下不成立。与 exp86（单步 PIMC / 策略切换）合读：**纯血线推理侧两条路线都关闭**。
- 净资产：`pmcpa_probe.py`（确定化自对弈 → 副本 PPO → 本局使用，0.3 s/局），可作为未来更强起点策略上的复测工具。

## Artifacts
| Path | Description |
|---|---|
| `experiments/probes/pmcpa_probe_K*_s*_lr*_seed*.json` | 五组结果 |

