diff --git a/common/utils.py b/common/utils.py
index 6a6f71b..109a681 100644
--- a/common/utils.py
+++ b/common/utils.py
@@ -167,7 +167,7 @@ def is_certificate_installed(cert_file:str) -> tuple[bool, str]:
         
         if sys.platform == "win32":
             # Use certutil to look up the certificate by its serial number in the Root store
-            cmd = ['certutil', '-store', 'Root', serial_number]
+            cmd = ['certutil', '-user', '-store', 'Root', serial_number]
             store_found_phrase = serial_number
         elif sys.platform == "darwin":
             # TODO test on MacOS
@@ -200,7 +200,7 @@ def install_root_cert(cert_file:str):
     # Install cert. If the cert exists, system will skip installation
     if sys.platform == "win32":
         print(f'"{cert_file}"')
-        full_command = ["certutil","-addstore","Root",cert_file]
+        full_command = ["certutil","-user","-addstore","Root",cert_file]
         # full_command = [
         #     'powershell', '-Command', 
         #     f"Start-Process 'certutil' -ArgumentList '-addstore', 'Root', '{cert_file}' -Wait -Verb 'RunAs';"
diff --git a/game/browser.py b/game/browser.py
index ac8a0ee..bb2d37a 100644
--- a/game/browser.py
+++ b/game/browser.py
@@ -98,6 +98,7 @@ class GameBrowser:
                     chromium = playwright.chromium
                     self.context = chromium.launch_persistent_context(
                         user_data_dir=utils.sub_folder(Folder.BROWSER_DATA),
+                        channel="chrome",
                         headless=False,
                         viewport={'width': self.width, 'height': self.height},
                         proxy=proxy_object,
@@ -118,6 +119,7 @@ class GameBrowser:
                     chromium = playwright.chromium
                     self.context = chromium.launch_persistent_context(
                         user_data_dir=utils.sub_folder(Folder.BROWSER_DATA),
+                        channel="chrome",
                         headless=False,
                         viewport={'width': self.width, 'height': self.height},
                         proxy=proxy_object,
diff --git a/mitm.py b/mitm.py
index 3fa23d2..19a9582 100644
--- a/mitm.py
+++ b/mitm.py
@@ -172,6 +172,8 @@ class MitmController:
             with_termlog=False,
             with_dumper=False,
         )
+        # pass large bodies through without buffering (game wasm.gz is gzip-encoded, 46MB decoded)
+        self.dump_master.options.update(stream_large_bodies="1m")
         try:
             LOGGER.info("Starting mitm server%s, proxy=%s", up_log_str, self.proxy_str)
             self.dump_master.addons.add(self.ws_data_addon)
